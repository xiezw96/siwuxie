spring:
  devtools:
    restart:
      enabled: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/siwuxie
    username: root
    password: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: true
    show-sql: true
    hibernate:
      ddl-auto: update

logging:
  level:
    root: info

siwuxie:
  localcache:
    capacity: 3000
  rediscache:
    host: localhost
    port: 6379
    databese: 0
    password:
